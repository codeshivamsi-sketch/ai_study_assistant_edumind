# Architecture

Generated by /arch on 2026-07-25.

## High-Level Architecture

```mermaid
flowchart LR
    Client(["Client"])

    subgraph Compose ["docker-compose.yml"]
        core["core-api\nFastAPI :8000"]
        worker["worker\nCelery"]
        redis[("redis\n:6379")]
        agentic["agentic\nFastAPI+LangGraph :8002"]
        neo4j[("neo4j\n:7475 / :7688")]
        notif["notifications\nFastify+gRPC :5000 / :5001"]
        postgres[("postgres\n:5432")]
        prometheus["prometheus\n:9090"]
        grafana["grafana\n:3000"]
    end

    Claude[["Anthropic Claude API"]]

    Client -->|"HTTP"| core
    Client -->|"HTTP"| notif
    Client -->|"HTTP"| agentic

    core -->|"send_task: notify_quiz_ready, generate_quiz"| redis
    core -->|"POST /upload (forward document)\nPOST /agent (chat Q&A)"| agentic
    core -->|"SQL via asyncpg"| postgres

    worker -->|"consumes queued tasks"| redis
    worker -.->|"gRPC NotifyQuizReady"| notif
    worker -->|"POST /agent (generate_quiz)"| agentic

    agentic -->|"Bolt"| neo4j
    agentic -->|"HTTPS"| Claude

    notif -->|"SQL via Prisma"| postgres
    notif -->|"BullMQ queue"| redis

    prometheus -.->|"scrapes /metrics"| core
    grafana -->|"queries"| prometheus
```

## ER Diagram

```mermaid
erDiagram
    USERS ||--o{ DOCUMENTS : "user_id, CASCADE"
    DOCUMENTS ||--o{ QUIZZES : "document_id, CASCADE"
    QUIZZES ||--o{ QUIZ_ATTEMPTS : "quiz_id, RESTRICT ⛔"
    USERS ||--o{ QUIZ_ATTEMPTS : "user_id, RESTRICT ⛔"
    USERS ||--o{ NOTIFICATIONS : "user_id, CASCADE"
    USERS ||--o{ CHATS : "user_id, CASCADE"
    DOCUMENTS ||--o{ CHATS : "document_id, CASCADE"
    CHATS ||--o{ MESSAGES : "chat_id, CASCADE"

    USERS {
        uuid id PK
        string email UK
        string name
        timestamptz created_at
    }
    DOCUMENTS {
        uuid id PK
        uuid user_id FK
        string title
        string status
        timestamptz created_at
    }
    QUIZZES {
        uuid id PK
        uuid document_id FK
        string topic
        jsonb questions
        timestamptz created_at
    }
    QUIZ_ATTEMPTS {
        uuid id PK
        uuid quiz_id FK
        uuid user_id FK
        jsonb answers
        numeric score
        timestamptz created_at
    }
    NOTIFICATIONS {
        uuid id PK
        uuid user_id FK
        uuid quiz_id
        text message
        boolean read
        timestamp created_at
    }
    CHATS {
        uuid id PK
        uuid user_id FK
        uuid document_id FK
        string title
        timestamptz created_at
    }
    MESSAGES {
        uuid id PK
        uuid chat_id FK
        string role
        string content
        timestamptz created_at
    }
```

For a single endpoint's call trace, run `/arch show flow [endpoint]` (mindmap of calls). For its sequence diagram, run `/arch sequence [endpoint]`.
