# Architecture

Generated by /arch on 2026-07-20.

## High-Level Architecture

```mermaid
flowchart LR
    Client(["Client"])

    subgraph Compose ["docker-compose.yml"]
        core["core-api\nFastAPI, port 8000"]
        worker["worker\nCelery, no port"]
        notif["notifications\nFastify+gRPC, port 5000 HTTP / 5001 gRPC"]
        postgres[("postgres\nport 5432")]
        redis[("redis\nport 6379")]
        prometheus["prometheus\nport 9090"]
        grafana["grafana\nport 3000"]
    end

    Client -->|HTTP :8000| core
    Client -->|HTTP :5000| notif

    core -->|SQL, asyncpg| postgres
    core -->|"send_task('notify_quiz_ready'), broker db 0"| redis

    worker -->|"consumes 'notify_quiz_ready', broker db 0"| redis
    worker -.->|"gRPC NotifyQuizReady :5001"| notif

    notif -->|SQL, Prisma| postgres
    notif -->|BullMQ db 1| redis

    prometheus -.->|scrapes /metrics| core
    grafana -->|queries| prometheus
```

## ER Diagram

```mermaid
erDiagram
    USERS ||--o{ DOCUMENTS : "user_id, CASCADE"
    DOCUMENTS ||--o{ QUIZZES : "document_id, CASCADE"
    QUIZZES ||--o{ QUIZ_ATTEMPTS : "quiz_id, RESTRICT ⛔"
    USERS ||--o{ QUIZ_ATTEMPTS : "user_id, RESTRICT ⛔"
    USERS ||--o{ NOTIFICATIONS : "user_id, CASCADE"

    USERS {
        uuid id PK
        string email UK
        string name
        timestamptz created_at
    }
    DOCUMENTS {
        uuid id PK
        uuid user_id FK
        string title
        string status
        timestamptz created_at
    }
    QUIZZES {
        uuid id PK
        uuid document_id FK
        string topic
        jsonb questions
        timestamptz created_at
    }
    QUIZ_ATTEMPTS {
        uuid id PK
        uuid quiz_id FK
        uuid user_id FK
        jsonb answers
        numeric score
        timestamptz created_at
    }
    NOTIFICATIONS {
        uuid id PK
        uuid user_id FK
        uuid quiz_id
        text message
        boolean read
        timestamp created_at
    }
```

For a single endpoint's call trace, run `/arch show flow [endpoint]` (mindmap of calls). For its sequence diagram, run `/arch sequence [endpoint]`.
