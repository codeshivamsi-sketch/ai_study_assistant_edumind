# Architecture

Generated by /arch on 2026-07-18.

## Code Structure

Modules grouped by responsibility, not folder — `core-api` (Python/FastAPI) plus the in-progress `notifications` service (Node/Prisma/gRPC).

```mermaid
mindmap
  root((EduMind))
    core-api Service :8000
      API layer
        routes.py: documents CRUD, quizzes CRUD, POST quiz_attempts, GET quizzes/id/stats
      Domain
        models.py: User, Document, Quiz, QuizAttempt
      Identity
        identity.py: get_current_user, X-User-Id header, 401 on missing/unknown
      Persistence
        database.py: async Postgres session, get_db shared by routes and identity
        Alembic: tables users, documents, quizzes, quiz_attempts, default version_table
      Async jobs
        worker.py: Celery app configured, no task registered
      gRPC codegen
        notifications_pb2 / notifications_pb2_grpc: generated client stubs, not yet called from routes.py
      Observability
        logger.py: structlog JSON logs
        metrics endpoint via prometheus-fastapi-instrumentator
    notifications Service (Node/TypeScript, in progress)
      Identity
        identity.ts: requireUser, X-User-Id header, raw SQL against core-api's users table
      Persistence
        prisma/schema.prisma: Notification model only, no User model/relation
        prisma/migrations: baseline + create_notifications (hand-written FK to users)
      Not yet wired
        no HTTP/gRPC server entrypoint, no job processor
    Shared infrastructure
      Postgres :5432
        single edumind database, shared by core-api (Alembic) and notifications (Prisma)
      Redis :6379
        Celery broker/backend, wired but idle
      Docker Compose
        healthchecks, depends_on ordering, builds core-api and worker images (notifications not yet added)
      Makefile
        up runs docker-compose up --build, down, logs
    Observability stack
      Prometheus :9090
        scrapes core-api:8000
      Grafana :3000
        dashboards, request rate and p90 latency
```

## Database Schema

`users`/`documents`/`quizzes`/`quiz_attempts` are Alembic-owned; `notifications` is Prisma-owned, joined only by a hand-written FK.

```mermaid
erDiagram
    USERS ||--o{ DOCUMENTS : owns
    DOCUMENTS ||--o{ QUIZZES : contains
    QUIZZES ||--o{ QUIZ_ATTEMPTS : "attempted via"
    USERS ||--o{ QUIZ_ATTEMPTS : attempts
    USERS ||--o{ NOTIFICATIONS : notifies

    USERS {
        uuid id PK
        string email UK
        string name
        timestamptz created_at
    }
    DOCUMENTS {
        uuid id PK
        uuid user_id FK
        string title
        string status
        timestamptz created_at
    }
    QUIZZES {
        uuid id PK
        uuid document_id FK
        string topic
        jsonb questions
        timestamptz created_at
    }
    QUIZ_ATTEMPTS {
        uuid id PK
        uuid quiz_id FK
        uuid user_id FK
        jsonb answers
        numeric score
        timestamptz created_at
    }
    NOTIFICATIONS {
        uuid id PK
        uuid user_id FK
        uuid quiz_id
        text message
        boolean read
        timestamp created_at
    }
```

## Delete Behavior

```mermaid
flowchart TD
    USERS[(users)]
    DOCUMENTS[(documents)]
    QUIZZES[(quizzes)]
    QUIZ_ATTEMPTS[(quiz_attempts)]
    NOTIFICATIONS[(notifications)]

    USERS -->|CASCADE| DOCUMENTS
    DOCUMENTS -->|CASCADE| QUIZZES
    QUIZZES -.->|"RESTRICT ⛔"| QUIZ_ATTEMPTS
    USERS -.->|"RESTRICT ⛔"| QUIZ_ATTEMPTS
    USERS -->|CASCADE| NOTIFICATIONS
```

Legend: solid `CASCADE` — deleting the parent row deletes its children too. Dashed `RESTRICT ⛔` — deleting the parent is blocked at the DB level while child rows exist.

## Data Access Map

Every endpoint below is in `core-api`; `notifications` has no HTTP/gRPC endpoints wired up yet.

```mermaid
flowchart LR
    subgraph Endpoints
        E1["POST /documents"]
        E2["GET /documents"]
        E3["GET /documents/{id}"]
        E4["PATCH /documents/{id}"]
        E5["DELETE /documents/{id}"]
        E6["POST /quizzes"]
        E7["GET /quizzes"]
        E8["GET /quizzes/{id}"]
        E9["PATCH /quizzes/{id}"]
        E10["DELETE /quizzes/{id}"]
        E11["POST /quiz_attempts"]
        E12["GET /quizzes/{id}/stats"]
    end

    USERS[(users)]
    DOCUMENTS[(documents)]
    QUIZZES[(quizzes)]
    QUIZ_ATTEMPTS[(quiz_attempts)]

    E1 -->|R: identity| USERS
    E1 -->|W| DOCUMENTS
    E2 -->|R: identity| USERS
    E2 -->|R| DOCUMENTS
    E3 -->|R: identity| USERS
    E3 -->|R| DOCUMENTS
    E4 -->|R: identity| USERS
    E4 -->|RW| DOCUMENTS
    E5 -->|R: identity| USERS
    E5 -->|RW| DOCUMENTS
    E6 -->|R: identity| USERS
    E6 -->|R: ownership| DOCUMENTS
    E6 -->|W| QUIZZES
    E7 -->|R: identity| USERS
    E7 -->|R: join| DOCUMENTS
    E7 -->|R| QUIZZES
    E8 -->|R: identity| USERS
    E8 -->|R: join| DOCUMENTS
    E8 -->|R| QUIZZES
    E9 -->|R: identity| USERS
    E9 -->|R: join| DOCUMENTS
    E9 -->|RW| QUIZZES
    E10 -->|R: identity| USERS
    E10 -->|R: join| DOCUMENTS
    E10 -->|RW| QUIZZES
    E11 -->|R: identity| USERS
    E11 -->|R: join| DOCUMENTS
    E11 -->|R: ownership| QUIZZES
    E11 -->|W| QUIZ_ATTEMPTS
    E12 -->|R: identity| USERS
    E12 -->|R: join| DOCUMENTS
    E12 -->|R: ownership| QUIZZES
    E12 -->|R: aggregate| QUIZ_ATTEMPTS
```

## Deployment

Services and networking as defined in `docker-compose.yml`; `notifications` isn't added to it yet.

```mermaid
flowchart LR
    subgraph Compose ["docker-compose.yml"]
        postgres[("postgres:16\nport 5432")]
        redis[("redis:7\nport 6379")]
        core["core-api\nbuild ./services/core-api\nport 8000"]
        worker["worker\nbuild ./services/core-api\ncelery worker"]
        prometheus["prometheus\nport 9090"]
        grafana["grafana\nport 3000"]
    end

    core -->|depends_on: service_healthy| postgres
    core -->|depends_on: service_started| redis
    worker -->|depends_on| redis
    worker -->|depends_on| postgres
    prometheus -.->|scrapes :8000/metrics| core
    grafana -->|depends_on| prometheus
```

## Request Flow

`POST /quiz_attempts` — deepest write chain in the codebase (identity check, then an ownership join through `quizzes`→`documents`, then the insert).

```mermaid
sequenceDiagram
    participant C as Client
    participant R as routes.create_quiz_attempt
    participant I as identity.get_current_user
    participant O as routes._get_owned_quiz
    participant DB as Postgres (edumind)

    C->>R: POST /quiz_attempts {quiz_id, answers, score}
    R->>I: Depends(get_current_user)
    I->>DB: SELECT * FROM users WHERE id = X-User-Id
    DB-->>I: user row (or 401 if missing/unknown)
    I-->>R: user
    R->>O: _get_owned_quiz(quiz_id, user, db)
    O->>DB: SELECT quizzes JOIN documents ON document_id = documents.id WHERE quiz.id = ? AND documents.user_id = ?
    DB-->>O: quiz row (or 404 if not found/not owned)
    O-->>R: quiz
    R->>DB: INSERT INTO quiz_attempts (...)
    R->>DB: COMMIT
    DB-->>R: attempt row (refreshed)
    R-->>C: 200 {attempt}
```
