# Architecture

Generated by /arch on 2026-07-27 (`/arch` itself no longer exists upstream —
see CLAUDE.md's Gotchas — this ER diagram was regenerated by hand from the
current `services/core-api/models.py` and
`services/notifications/prisma/schema.prisma`, cross-checked against every
migration file, not just the ORM/schema source).

## ER Diagram

```mermaid
erDiagram
    USERS ||--o{ DOCUMENTS : "user_id, CASCADE"
    DOCUMENTS ||--o{ QUIZZES : "document_id, CASCADE"
    QUIZZES ||--o{ QUIZ_ATTEMPTS : "quiz_id, RESTRICT ⛔"
    USERS ||--o{ QUIZ_ATTEMPTS : "user_id, RESTRICT ⛔"
    USERS ||--o{ CHATS : "user_id, CASCADE"
    DOCUMENTS ||--o{ CHATS : "document_id, CASCADE"
    CHATS ||--o{ MESSAGES : "chat_id, CASCADE"
    USERS ||--o{ NOTIFICATIONS : "user_id, CASCADE"
    CHATS ||--o{ NOTIFICATIONS : "chat_id (nullable), CASCADE"
    MESSAGES ||--o{ NOTIFICATIONS : "message_id (nullable), CASCADE"

    USERS {
        uuid id PK
        string email UK
        string name
        timestamptz created_at
    }
    DOCUMENTS {
        uuid id PK
        uuid user_id FK
        string title
        string status
        timestamptz created_at
    }
    QUIZZES {
        uuid id PK
        uuid document_id FK
        string topic
        jsonb questions
        timestamptz created_at
    }
    QUIZ_ATTEMPTS {
        uuid id PK
        uuid quiz_id FK
        uuid user_id FK
        jsonb answers
        numeric score
        timestamptz created_at
    }
    CHATS {
        uuid id PK
        uuid user_id FK
        uuid document_id FK
        string title
        timestamptz created_at
    }
    MESSAGES {
        uuid id PK
        uuid chat_id FK
        string role
        string content
        timestamptz created_at
    }
    NOTIFICATIONS {
        uuid id PK
        uuid user_id FK
        uuid quiz_id "nullable, no DB-level FK"
        uuid chat_id FK "nullable"
        uuid message_id FK "nullable"
        text message
        boolean read
        timestamp created_at
    }
```

Two things worth knowing about `NOTIFICATIONS` that the diagram's edges
alone don't make obvious:
- `quiz_id` was never a real foreign key at the database level (even before
  it became nullable) — it's a plain `UUID` column, application-enforced
  only. `chat_id`/`message_id`, added when the notify pipeline was
  generalized for chat answers, **are** real `CASCADE` foreign keys into
  `chats`/`messages`.
- A given `NOTIFICATIONS` row now has either `quiz_id` set (a quiz-ready
  notification) or `chat_id`/`message_id` set (a chat-answer notification),
  never a dedicated `kind` column to say which — the populated column *is*
  the discriminator.

## Request Flow — POST /documents/{document_id}/upload

Hand-saved on 2026-07-26 (the `/arch sequence [endpoint]` command that used
to generate this on demand was removed upstream — see CLAUDE.md's Gotchas —
so this is a manually captured, point-in-time trace, not auto-regenerating).

```mermaid
sequenceDiagram
    participant C as Client
    participant API as core-api :8000
    participant PG as Postgres (documents)
    participant A as agentic :8002
    participant D as uploads/ disk
    participant CH as Chroma (pdf_chunks)
    participant N as Neo4j
    participant CL as Anthropic Claude API

    C->>API: POST /documents/{document_id}/upload (multipart file), X-User-Id
    API->>PG: SELECT documents WHERE id=? AND user_id=?
    PG-->>API: document row
    alt not found / not owned
        API-->>C: 404 Document not found
    end
    API->>API: content = await file.read()
    API->>PG: UPDATE documents SET status='processing'
    PG-->>API: commit ack
    API->>A: POST /upload (files={file: filename,content}, data={document_id}), timeout=120s

    A->>A: save_pdf_on_disk — os.path.basename(filename) [strips path traversal]
    A->>D: write uploads/<safe_filename>
    A->>A: get_pdf_content — PdfReader, extract_text() per page
    A->>D: read uploads/<safe_filename>
    A->>A: split_content_into_chunks — RecursiveCharacterTextSplitter(500, overlap 50)
    A->>A: embed_chunks — SentenceTransformer("all-MiniLM-L6-v2").encode(chunks)
    A->>CH: collection.add(documents=chunks, embeddings, ids=f"{document_id}-{i}", metadatas={document_id})

    loop ingest_graph — once per chunk
        A->>CL: extract_entities(chunk) — messages.create(claude-opus-4-5)
        CL-->>A: {entities:[...], relationships:[[a,rel,b]...]} (regex-extracted JSON)
        A->>N: MERGE (e:Concept {name}) per entity
        A->>N: MATCH a, MATCH b, MERGE (a)-[:RELATES_TO]->(b) per relationship
    end

    A-->>API: 200 {filename, chunks: len(chunks), document_id}

    alt 2xx from agentic
        API->>PG: UPDATE documents SET status='ready'
        PG-->>API: commit ack
        API->>PG: refresh(document)
        API->>API: log.info("document_uploaded", document_id)
        API-->>C: 200 document (status=ready)
    else non-2xx / exception (raise_for_status or network error)
        API->>PG: UPDATE documents SET status='failed'
        PG-->>API: commit ack
        API->>API: log.warning("document_upload_failed", document_id, error)
        API-->>C: 502 Document ingestion failed
    end
```

Notable detail: `extract_entities` is a real Claude API call made once per
chunk, sequentially inside the loop — a large PDF means many blocking
round-trips to Anthropic before the upload response returns, all within the
120s httpx timeout core-api holds open to agentic.

## Request Flow — POST /chats/{chat_id}/messages

Hand-saved on 2026-07-26 (same caveat as the upload flow above — manually
captured, not auto-regenerating).

```mermaid
sequenceDiagram
    participant C as Client
    participant API as core-api :8000
    participant PG as Postgres (chats/messages)
    participant A as agentic :8002
    participant CH as Chroma (pdf_chunks)
    participant N as Neo4j
    participant CL as Anthropic Claude API
    participant SQ as checkpoints.db (SQLite)

    C->>API: POST /chats/{chat_id}/messages {content}, X-User-Id
    API->>PG: SELECT chats WHERE id=? AND user_id=?
    PG-->>API: chat row
    alt not found / not owned
        API-->>C: 404 Chat not found
    end
    API->>PG: INSERT messages (role='user', content), COMMIT
    PG-->>API: user_message row
    Note right of API: user message durably committed before the agentic call —<br/>a downstream failure still leaves it persisted for retry

    API->>A: POST /agent {question: content, document_id: chat.document_id}, timeout=60s
    A->>A: thread_id = uuid4(); config={configurable:{thread_id}}
    A->>A: agent.invoke(...) — LangGraph orchestrator entry point

    A->>CL: orchestrator_node — classify intent ("answer"/"quiz"/"summarize"), max_tokens=50
    CL-->>A: intent

    A->>A: retrieval_node
    A->>A: embed_ques(question) — local SentenceTransformer
    A->>CH: collection.query(embedding, n_results=3, where={document_id})
    CH-->>A: chunks
    A->>CL: extract_entities(question) — for get_related_from_graph
    CL-->>A: entities JSON
    A->>N: get_related_concepts(entity) per entity — MATCH (a)-[:RELATES_TO]->(b)
    N-->>A: related_concepts

    alt intent == answer
        A->>CL: answer_node — get_ans_from_claud(question, chunks, related_concepts)
        CL-->>A: answer text
    else intent == quiz
        A->>CL: quiz_node — generate 3 quiz questions from chunks
        CL-->>A: quiz_questions
        Note right of A: interrupt_after=["quiz"] — graph pauses here,<br/>never reaches evaluate_node in this flow
    else intent == summarize
        A->>CL: summarizer_node — summarize chunks into study notes
        CL-->>A: summary
    end

    A->>SQ: checkpointer persists graph state under thread_id
    A-->>API: 200 {...state, intent, thread_id} (answer | quiz_questions | summary)

    alt agentic call succeeded
        API->>API: ask_question — data.get("answer") if present,<br/>else json.dumps(state minus question/document_id)
        API->>PG: INSERT messages (role='assistant', content=answer), COMMIT
        PG-->>API: assistant_message row
        API->>API: log.info("chat_message_created", chat_id, user_id)
        API-->>C: 200 {user_message, assistant_message}
    else non-2xx / exception
        API->>API: log.warning("chat_message_agentic_failed", chat_id, error)
        API-->>C: 502 Failed to get an answer
        Note right of API: no assistant row inserted — user_message row<br/>from above stays, client can retry
    end
```

Notable details: three separate Claude calls happen per message (intent
classification, entity extraction for the graph lookup, and the
answer/quiz/summary generation itself) — not one. And unlike `/upload`,
`ask_question`'s fallback path (`quiz`/`summarize` intents) stores the
*entire* non-answer response body as JSON text in `content`, including the
raw retrieved `chunks` — not a clean quiz or summary string (see
`services/agentic/CLAUDE.md` and this repo's chat-feature plan for why).

## Request Flow — how `quizzes` and `quiz_attempts` get populated

Hand-saved on 2026-07-26 (same caveat as the flows above — manually
captured, not auto-regenerating). Covers both ways a `quizzes` row can be
created, the full Celery/Redis → gRPC → BullMQ → Prisma notify chain
triggered by either one, and — separately — how `quiz_attempts` is
populated (spoiler: no queue is involved there at all).

```mermaid
sequenceDiagram
    participant C as Client
    participant API as core-api :8000
    participant PG as Postgres edumind (documents/quizzes/quiz_attempts/notifications)
    participant R as Redis db0 (Celery broker+backend)
    participant W as Celery worker
    participant A as agentic :8002
    participant GS as notifications gRPC :5001
    participant RQ as Redis db1 (BullMQ)
    participant NW as notifications BullMQ worker

    rect rgb(230,245,255)
    Note over C,R: Path A — POST /quizzes (synchronous, caller supplies questions directly)
    C->>API: POST /quizzes {document_id, topic, questions}, X-User-Id
    API->>PG: SELECT documents WHERE id=? AND user_id=? (404 if not owned)
    API->>PG: INSERT quizzes (document_id, topic, questions), COMMIT
    PG-->>API: quiz row
    API->>R: send_task("notify_quiz_ready", args=[user_id, quiz_id])
    Note right of API: fire-and-forget — dispatch failure is logged,<br/>never fails the 200 response
    API-->>C: 200 quiz
    end

    rect rgb(255,245,230)
    Note over C,A: Path B — POST /quizzes/generate (async, agentic generates the questions)
    C->>API: POST /quizzes/generate {document_id, topic}, X-User-Id
    API->>PG: SELECT documents WHERE id=? AND user_id=? (404 if not owned)
    alt document.status != "ready"
        API-->>C: 409 Document not ready for quiz generation
    end
    API->>R: send_task("generate_quiz", args=[user_id, document_id, topic])
    R-->>API: task_id
    API-->>C: 202 {job_id: task_id}
    Note right of C: no polling endpoint exists for job_id —<br/>the quiz just shows up later via GET /quizzes or the notification

    R->>W: worker pulls "generate_quiz" off the broker queue
    W->>A: POST /agent {question: "Quiz me on {topic}", document_id} (sync httpx, 60s)
    A-->>W: {quiz_questions: [...]}
    alt agentic call raises
        W->>W: self.retry(countdown=GENERATE_QUIZ_RETRY_COUNTDOWN), max_retries=2
        Note right of W: once retries are exhausted
        W->>PG: UPDATE documents SET status='failed'
    else agentic call succeeds
        W->>PG: INSERT quizzes (fresh engine+session per call — separate event loop, see worker.py), COMMIT
        PG-->>W: quiz_id
        alt insert raises
            W->>PG: UPDATE documents SET status='failed'
        else insert succeeds
            W->>R: send_task("notify_quiz_ready", args=[user_id, quiz_id])
        end
    end
    end

    rect rgb(235,255,235)
    Note over R,PG: Notify chain — dispatched by either path above, runs identically either way
    R->>W: worker pulls "notify_quiz_ready" off the broker queue
    W->>GS: gRPC NotifyQuizReady(user_id, quiz_id), timeout=5s
    GS->>RQ: enqueueNotifyQuizReady — BullMQ Queue("NotifyQuizReady").add(payload, attempts=3, backoff exponential 1000ms)
    RQ-->>GS: job queued
    GS-->>W: {accepted: true}
    alt gRPC call raises
        W->>W: self.retry(countdown=NOTIFY_QUIZ_READY_RETRY_COUNTDOWN), max_retries=3
    end

    RQ->>NW: BullMQ worker pulls the "NotifyQuizReady" job
    NW->>PG: prisma.notification.create({user_id, quiz_id, message}) — INSERT notifications
    PG-->>NW: notification row
    alt job exhausts all 3 BullMQ attempts
        NW->>NW: emit "failed" event → logger.error (no notification row ever created)
    end
    end

    rect rgb(245,235,255)
    Note over C,PG: quiz_attempts — entirely separate: synchronous, client-driven, no Celery/Redis involved at all
    C->>API: POST /quiz_attempts {quiz_id, answers, score}, X-User-Id
    API->>PG: SELECT quizzes JOIN documents WHERE quiz.id=? AND documents.user_id=? (404 if not owned)
    API->>PG: INSERT quiz_attempts (quiz_id, user_id, answers, score), COMMIT
    PG-->>API: attempt row
    API-->>C: 200 attempt
    Note right of API: score is client-supplied — nothing here calls<br/>agentic to grade it, and no queue is involved
    end
```

Notable details: `notify_quiz_ready` is dispatched to the **same Redis
instance, logical DB 0** that Celery already uses as its broker/backend;
`notifications`' BullMQ queue uses **the same Redis instance, logical DB
1** (`REDIS_URL: redis://redis:6379/1` in `docker-compose.yml`) — one Redis
container, two separate queueing systems on two different DB indexes,
never interacting with each other directly (the only bridge between them is
the synchronous gRPC call). `_insert_quiz`/`_mark_document_failed` in
`worker.py` deliberately open a **fresh SQLAlchemy engine per call** instead
of reusing `database.py`'s shared engine — each Celery task body runs
`asyncio.run()` on its own fresh event loop, and asyncpg connections are
bound to the loop that created them.

For a single endpoint's call trace, the old `/arch show flow [endpoint]`
command (mindmap of calls) and `/arch sequence [endpoint]` command no longer
exist — see CLAUDE.md's Gotchas.
