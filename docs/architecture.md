# Architecture

Generated by /arch on 2026-07-17. Architecture, Schema, and Data Access Map are fully regenerated — do not hand-edit them. Delete Behavior is different: any hand-written prose you add below its diagram/legend is preserved across future `/arch` runs.

> **Note:** built from direct codebase analysis. `codegraph` (registered in `.mcp.json`) did not surface any tools this session — it needs a Claude Code restart and your approval of the project MCP server first. Regenerate this doc once that's available for a codegraph-verified pass.

## Architecture

```mermaid
mindmap
  root((EduMind core-api))
    core-api Service :8000
      API layer
        routes.py: documents CRUD, quizzes CRUD, POST quiz_attempts, GET quizzes/id/stats
      Domain
        models.py: User, Document, Quiz, QuizAttempt
      Identity
        identity.py: get_current_user, X-User-Id header, 401 on missing/unknown
      Persistence
        database.py: async Postgres session, get_db shared by routes and identity
        Alembic: tables users, documents, quizzes, quiz_attempts, default version_table
      Async jobs
        worker.py: Celery app configured, no task registered
      Observability
        logger.py: structlog JSON logs
        metrics endpoint via prometheus-fastapi-instrumentator
    Shared infrastructure
      Postgres :5432
        single edumind database
      Redis :6379
        Celery broker and backend, wired but idle
      Docker Compose
        healthchecks, depends_on ordering, builds core-api and worker images
      Makefile
        up runs docker-compose up --build, down, logs
    Observability stack
      Prometheus :9090
        scrapes core-api:8000, no global scrape_interval override so Prometheus's built-in default applies
      Grafana :3000
        dashboards, request rate and p90 latency
```

## Schema

```mermaid
erDiagram
    USERS ||--o{ DOCUMENTS : owns
    DOCUMENTS ||--o{ QUIZZES : contains
    QUIZZES ||--o{ QUIZ_ATTEMPTS : "attempted via"
    USERS ||--o{ QUIZ_ATTEMPTS : attempts

    USERS {
        uuid id PK
        string email UK
        string name
        timestamptz created_at
    }
    DOCUMENTS {
        uuid id PK
        uuid user_id FK
        string title
        string status
        timestamptz created_at
    }
    QUIZZES {
        uuid id PK
        uuid document_id FK
        string topic
        jsonb questions
        timestamptz created_at
    }
    QUIZ_ATTEMPTS {
        uuid id PK
        uuid quiz_id FK
        uuid user_id FK
        jsonb answers
        numeric score
        timestamptz created_at
    }
```

## Data Access Map

```mermaid
flowchart LR
    subgraph Endpoints
        E1["POST /documents"]
        E2["GET /documents"]
        E3["GET /documents/{id}"]
        E4["PATCH /documents/{id}"]
        E5["DELETE /documents/{id}"]
        E6["POST /quizzes"]
        E7["GET /quizzes"]
        E8["GET /quizzes/{id}"]
        E9["PATCH /quizzes/{id}"]
        E10["DELETE /quizzes/{id}"]
        E11["POST /quiz_attempts"]
        E12["GET /quizzes/{id}/stats"]
    end

    USERS[(users)]
    DOCUMENTS[(documents)]
    QUIZZES[(quizzes)]
    QUIZ_ATTEMPTS[(quiz_attempts)]

    E1 -->|R: identity| USERS
    E1 -->|W| DOCUMENTS
    E2 -->|R: identity| USERS
    E2 -->|R| DOCUMENTS
    E3 -->|R: identity| USERS
    E3 -->|R| DOCUMENTS
    E4 -->|R: identity| USERS
    E4 -->|RW| DOCUMENTS
    E5 -->|R: identity| USERS
    E5 -->|RW| DOCUMENTS
    E6 -->|R: identity| USERS
    E6 -->|R: ownership| DOCUMENTS
    E6 -->|W| QUIZZES
    E7 -->|R: identity| USERS
    E7 -->|R: join| DOCUMENTS
    E7 -->|R| QUIZZES
    E8 -->|R: identity| USERS
    E8 -->|R: join| DOCUMENTS
    E8 -->|R| QUIZZES
    E9 -->|R: identity| USERS
    E9 -->|R: join| DOCUMENTS
    E9 -->|RW| QUIZZES
    E10 -->|R: identity| USERS
    E10 -->|R: join| DOCUMENTS
    E10 -->|RW| QUIZZES
    E11 -->|R: identity| USERS
    E11 -->|R: join| DOCUMENTS
    E11 -->|R: ownership| QUIZZES
    E11 -->|W| QUIZ_ATTEMPTS
    E12 -->|R: identity| USERS
    E12 -->|R: join| DOCUMENTS
    E12 -->|R: ownership| QUIZZES
    E12 -->|R: aggregate| QUIZ_ATTEMPTS
```

Every endpoint reads `users` first via the `get_current_user` dependency (`identity.py`) — that's the `R: identity` edge on all twelve. Quiz and quiz_attempt endpoints additionally join back through `documents.user_id` to authorize (`R: join` / `R: ownership`) before touching their own table, per the ownership invariant in CLAUDE.md.

## Delete Behavior

```mermaid
flowchart TD
    USERS[(users)]
    DOCUMENTS[(documents)]
    QUIZZES[(quizzes)]
    QUIZ_ATTEMPTS[(quiz_attempts)]

    USERS -->|CASCADE| DOCUMENTS
    DOCUMENTS -->|CASCADE| QUIZZES
    QUIZZES -.->|"RESTRICT ⛔"| QUIZ_ATTEMPTS
    USERS -.->|"RESTRICT ⛔"| QUIZ_ATTEMPTS
```

Legend: solid `CASCADE` — deleting the parent row deletes its children too. Dashed `RESTRICT ⛔` — deleting the parent is blocked at the DB level while child rows exist.
