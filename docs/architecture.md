# Architecture

Generated by /arch on 2026-07-15. This file is regenerated — do not hand-edit.

> **Note:** built from direct codebase analysis. `codegraph` (registered in `.mcp.json`) did not surface any tools this session — it needs a Claude Code restart and your approval of the project MCP server first. Regenerate this doc once that's available for a codegraph-verified pass.

## Module map

```mermaid
mindmap
  root((CreditCore))
    Origination Service :8000
      API layer
        routes.py: POST /applications, GET /applications/id, PATCH submit, GET /tasks/id
      Domain
        models.py: LoanApplication, LoanStatus DRAFT/SUBMITTED
      Persistence
        database.py: async Postgres session
        Alembic: table loan_applications, default version_table
      Async jobs
        worker.py: Celery task run_credit_check, simulated score
      Eventing
        kafka_producer.py: publishes loan.submitted
        kafka_consumer.py: also consumes loan.submitted, group origination-group
      Observability
        logger.py: structlog JSON logs
        metrics endpoint via prometheus-fastapi-instrumentator
    Ledger Service :8001
      API layer
        routes.py: POST /postings, GET /postings/loan_id
      Domain
        models.py: LedgerEntry, EntryType DEBIT/CREDIT
      Persistence
        database.py: async Postgres session
        Alembic: table ledger_entries, version_table alembic_version_ledger
      Observability
        metrics endpoint via prometheus-fastapi-instrumentator
      No Kafka integration despite README diagram
    Shared infrastructure
      Postgres :5432
        single creditcore database shared by both services
      Redis :6379
        Celery broker and backend, Origination only
      Kafka KRaft :9092
        topic loan.submitted, both producer and consumer live in Origination
      Docker Compose
        healthchecks, depends_on ordering, builds both service images
      Makefile
        up runs docker-compose up --build, down, logs
    Observability stack
      Prometheus :9090
        scrapes origination:8000 and ledger:8001, no global scrape_interval override so Prometheus's built-in default applies
      Grafana :3000
        dashboards, request rate and p90 latency
```

## Actual data flow

```mermaid
flowchart TD
    Client(["Client"])

    Client -->|POST /applications, idempotency-key header| Orig[Origination :8000]
    Orig -->|insert loan_applications| PG[(Postgres: creditcore)]

    Client -->|PATCH /applications/id/submit| Orig
    Orig -->|publish loan.submitted| Kafka[/Kafka topic: loan.submitted/]
    Kafka -.->|consumed by its own origination-group consumer, logs only| Orig
    Orig -->|celery_app.delay run_credit_check| Redis[(Redis)]
    Redis --> Celery[Celery worker]
    Celery -.->|result stored, not written back to loan_applications| Redis

    Client -->|POST /postings, GET /postings/loan_id — independent of the Kafka event| Ledger[Ledger :8001]
    Ledger -->|insert debit + credit LedgerEntry| PG

    Prom[Prometheus :9090] -.->|scrape /metrics| Orig
    Prom -.->|scrape /metrics| Ledger
    Prom --> Grafana[Grafana :3000]
```

**Discrepancy vs. README:** the top-level `README.md` diagram shows Kafka flowing from Origination into Ledger. That link doesn't exist in code — Ledger has no `kafka` dependency and no consumer. Origination's own consumer subscribes to the topic it publishes and only logs the message; nothing currently bridges the event to Ledger's `/postings` endpoint. Ledger entries are created solely by direct API calls.
